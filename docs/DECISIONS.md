# Decisions

- Use `.ai` as a git submodule rather than copying the template repo into the app tree.
- Keep Extend upstream history intact by preserving `upstream` and `origin` remotes separately.
- Build the app as FastAPI + Next.js inside the same repository as the SDK.
- Keep the UI inspired by Extend flow patterns but not visually identical to Extend.

